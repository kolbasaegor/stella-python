language core;

extend with
  #comparison-operators,
  #logical-operators,
  #natural-literals,
  #let-bindings,
  #panic;

fn main(n : Nat) -> Nat {
  return
    let n = 3 in
    let m = 4 in
    if true then panic! else 1
}
